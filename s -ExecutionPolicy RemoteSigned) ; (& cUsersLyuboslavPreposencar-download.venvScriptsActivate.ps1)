[1mdiff --git a/app/__init__.py b/app/__init__.py[m
[1mindex 349b7d1..74a09bd 100644[m
[1m--- a/app/__init__.py[m
[1m+++ b/app/__init__.py[m
[36m@@ -22,6 +22,9 @@[m [mPROTECTED_PREFIXES = [[m
 # Public paths that do not require authentication[m
 PUBLIC_PATHS = ["/login", "/logout"][m
 [m
[32m+[m[32m# Public prefixes for Buffer media (publicly accessible, no auth required)[m
[32m+[m[32mPUBLIC_PREFIXES = ["/buffer-media"][m
[32m+[m
 from .config import STATIC_DIR[m
 from .routes import router[m
 [m
[36m@@ -45,6 +48,11 @@[m [mdef create_app() -> FastAPI:[m
             if path in PUBLIC_PATHS or path.startswith("/static") or path.startswith("/favicon") or path.startswith("/site.webmanifest"):[m
                 return await call_next(request)[m
 [m
[32m+[m[32m            # Allow public prefixes (e.g., /buffer-media)[m
[32m+[m[32m            for public_prefix in PUBLIC_PREFIXES:[m
[32m+[m[32m                if path.startswith(public_prefix):[m
[32m+[m[32m                    return await call_next(request)[m
[32m+[m
             # If session indicates authenticated, allow[m
             sess = request.session[m
             if sess and sess.get("authenticated"):[m
[1mdiff --git a/app/config.py b/app/config.py[m
[1mindex 952719b..7e77e6c 100644[m
[1m--- a/app/config.py[m
[1m+++ b/app/config.py[m
[36m@@ -18,7 +18,7 @@[m [mHEADERS = {[m
 STATIC_FOOTER = """━━━━━━━━━━━━━━━━━━━[m
 ✅ Възможност за лизинг без първоначална вноска[m
 ✅ VIN проверка + пълен списък с екстри при интерес[m
[31m-✅ Доставка до 3–4 месеца – всичко организирано от нас[m
[32m+[m[32m✅ Доставка до 3 месеца – всичко организирано от нас[m
 ✅ Пълно съдействие до регистрация в България[m
 ━━━━━━━━━━━━━━━━━━━[m
 📲 Следвайте ни във Facebook за още предложения[m
[1mdiff --git a/app/routes.py b/app/routes.py[m
[1mindex 2048979..2e0f1ff 100644[m
[1m--- a/app/routes.py[m
[1m+++ b/app/routes.py[m
[36m@@ -13,15 +13,24 @@[m [mimport json[m
 import logging[m
 import threading[m
 import uuid[m
[32m+[m[32mimport secrets[m
 from enum import Enum[m
 from concurrent.futures import ThreadPoolExecutor[m
 from dataclasses import dataclass, field[m
 from typing import Optional, Dict[m
[32m+[m[32mfrom pathlib import Path[m
 [m
 logger = logging.getLogger("encar.facebook")[m
 [m
 router = APIRouter()[m
 [m
[32m+[m[32m# ============================================================================[m
[32m+[m[32m# Media token storage for Buffer public image URLs[m
[32m+[m[32m# ============================================================================[m
[32m+[m[32m# Maps token -> {job_id, image_names (list), expires_at}[m
[32m+[m[32m_media_tokens_lock = threading.Lock()[m
[32m+[m[32m_media_tokens: Dict[str, dict] = {}[m
[32m+[m
 # ============================================================================[m
 # Background job processing for concurrent Encar generation[m
 # ============================================================================[m
[36m@@ -231,97 +240,202 @@[m [mdef get_photos(job_id: str, request: Request):[m
     }[m
 [m
 [m
[32m+[m[32m@router.get("/buffer-media/{token}/{image_name}")[m
[32m+[m[32mdef get_buffer_media(token: str, image_name: str):[m
[32m+[m[32m    """Public endpoint to serve images via Buffer media token.[m
[32m+[m[41m    [m
[32m+[m[32m    This endpoint is NOT protected by authentication and is used by Buffer[m
[32m+[m[32m    to fetch images for posting. The token is cryptographically secure and[m
[32m+[m[32m    server-side validated to prevent unauthorized access.[m
[32m+[m[32m    """[m
[32m+[m[32m    with _media_tokens_lock:[m
[32m+[m[32m        if token not in _media_tokens:[m
[32m+[m[32m            raise HTTPException(status_code=404, detail="Media token not found or expired.")[m
[32m+[m[41m        [m
[32m+[m[32m        token_data = _media_tokens[token][m
[32m+[m[32m        job_id = token_data["job_id"][m
[32m+[m[32m        allowed_images = token_data["image_names"][m
[32m+[m[41m    [m
[32m+[m[32m    # Validate that the requested image is in the allowed list[m
[32m+[m[32m    if image_name not in allowed_images:[m
[32m+[m[32m        raise HTTPException(status_code=403, detail="Image not allowed for this token.")[m
[32m+[m[41m    [m
[32m+[m[32m    # Use the safe get_image_path to resolve and validate the path (prevents path traversal)[m
[32m+[m[32m    image_path = get_image_path(job_id, image_name)[m
[32m+[m
[32m+[m[32m    if not image_path.exists():[m
[32m+[m[32m        raise HTTPException(status_code=404, detail="Снимката не е намерена.")[m
[32m+[m
[32m+[m[32m    return FileResponse([m
[32m+[m[32m        image_path,[m
[32m+[m[32m        media_type="image/jpeg",[m
[32m+[m[32m        filename=image_name[m
[32m+[m[32m    )[m
[32m+[m
[32m+[m
[32m+[m[32m# ============================================================================[m
[32m+[m[32m# Helper functions for Buffer GraphQL integration[m
[32m+[m[32m# ============================================================================[m
[32m+[m
[32m+[m[32mdef generate_media_token(job_id: str, image_names: list) -> str:[m
[32m+[m[32m    """Generate a cryptographically secure media token and store metadata server-side."""[m
[32m+[m[32m    token = secrets.token_urlsafe(32)[m
[32m+[m[32m    with _media_tokens_lock:[m
[32m+[m[32m        _media_tokens[token] = {[m
[32m+[m[32m            "job_id": job_id,[m
[32m+[m[32m            "image_names": image_names,[m
[32m+[m[32m        }[m
[32m+[m[32m    return token[m
[32m+[m
[32m+[m
[32m+[m[32mdef build_buffer_media_url(request: Request, token: str, image_name: str) -> str:[m
[32m+[m[32m    """Build the public URL for a Buffer media image using the media token."""[m
[32m+[m[32m    base_url = str(request.base_url).rstrip("/")[m
[32m+[m[32m    return f"{base_url}/buffer-media/{token}/{image_name}"[m
[32m+[m
[32m+[m
[32m+[m[32mdef validate_buffer_credentials() -> tuple:[m
[32m+[m[32m    """Validate that Buffer API credentials are configured.[m
[32m+[m[41m    [m
[32m+[m[32m    Returns: (BUFFER_API_KEY, BUFFER_CHANNEL_ID)[m
[32m+[m[32m    Raises: HTTPException if credentials missing[m
[32m+[m[32m    """[m
[32m+[m[32m    api_key = os.environ.get("BUFFER_API_KEY")[m
[32m+[m[32m    channel_id = os.environ.get("BUFFER_CHANNEL_ID")[m
[32m+[m[41m    [m
[32m+[m[32m    if not api_key or not channel_id:[m
[32m+[m[32m        raise HTTPException([m
[32m+[m[32m            status_code=500,[m
[32m+[m[32m            detail="Buffer credentials not configured on server (BUFFER_API_KEY, BUFFER_CHANNEL_ID missing)."[m
[32m+[m[32m        )[m
[32m+[m[41m    [m
[32m+[m[32m    return api_key, channel_id[m
[32m+[m
[32m+[m
[32m+[m[32mdef call_buffer_graphql(api_key: str, mutation: str) -> dict:[m
[32m+[m[32m    """Call Buffer GraphQL API with a mutation.[m
[32m+[m[41m    [m
[32m+[m[32m    Args:[m
[32m+[m[32m        api_key: Buffer API key[m
[32m+[m[32m        mutation: GraphQL mutation string[m
[32m+[m[41m    [m
[32m+[m[32m    Returns: GraphQL response dict with data or errors[m
[32m+[m[32m    Raises: HTTPException on network or parsing errors[m
[32m+[m[32m    """[m
[32m+[m[32m    url = "https://api.buffer.com"[m
[32m+[m[32m    headers = {[m
[32m+[m[32m        "Authorization": f"Bearer {api_key}",[m
[32m+[m[32m        "Content-Type": "application/json",[m
[32m+[m[32m    }[m
[32m+[m[32m    payload = {"query": mutation}[m
[32m+[m[41m    [m
[32m+[m[32m    try:[m
[32m+[m[32m        resp = requests.post(url, json=payload, headers=headers, timeout=30)[m
[32m+[m[32m    except Exception as e:[m
[32m+[m[32m        raise HTTPException(status_code=502, detail=f"Buffer API connection failed: {str(e)}")[m
[32m+[m[41m    [m
[32m+[m[32m    try:[m
[32m+[m[32m        data = resp.json()[m
[32m+[m[32m    except Exception:[m
[32m+[m[32m        raise HTTPException(status_code=502, detail=f"Buffer API response parse failed: {resp.text}")[m
[32m+[m[41m    [m
[32m+[m[32m    return data[m
[32m+[m
[32m+[m
 @router.post("/publish-facebook")[m
 def publish_facebook(request: Request, body: dict):[m
[31m-    """Publish selected images and message to configured Facebook Page.[m
[32m+[m[32m    """Publish selected images and message to Buffer (now, via shareNow mode).[m
 [m
     Expects JSON body: { "job_id": "...", "image_ids": ["img1.jpg", ...], "message": "..." }[m
[32m+[m[41m    [m
[32m+[m[32m    Uses Buffer GraphQL API with createPost mutation. Images are served publicly via[m
[32m+[m[32m    cryptographically secure media tokens.[m
     """[m
     try:[m
[31m-        page_id = os.environ.get("FACEBOOK_PAGE_ID")[m
[31m-        page_token = os.environ.get("FACEBOOK_PAGE_ACCESS_TOKEN")[m
[31m-[m
[31m-        if not page_id or not page_token:[m
[31m-            raise HTTPException(status_code=500, detail="Facebook credentials not configured on server.")[m
[32m+[m[32m        # Validate Buffer credentials are configured[m
[32m+[m[32m        api_key, channel_id = validate_buffer_credentials()[m
 [m
         job_id = body.get("job_id")[m
         image_ids = body.get("image_ids") or [][m
         message = body.get("message") or ""[m
 [m
[31m-        # Publish each photo by uploading the local file (multipart/form-data)[m
[31m-        photo_fb_ids = [][m
[32m+[m[32m        # Validate job_id and that the job directory exists[m
[32m+[m[32m        if not job_id:[m
[32m+[m[32m            raise HTTPException(status_code=400, detail="job_id is required.")[m
[32m+[m
[32m+[m[32m        job_dir = GENERATED_DIR / job_id[m
[32m+[m[32m        if not job_dir.exists():[m
[32m+[m[32m            raise HTTPException(status_code=400, detail=f"Job directory not found: {job_id}")[m
 [m
[32m+[m[32m        # Validate that all selected images exist[m
         for image_name in image_ids:[m
[31m-            # resolve local image path (must already be downloaded into GENERATED_DIR/job_id)[m
             image_path = get_image_path(job_id, image_name)[m
             if not image_path.exists() or not image_path.is_file():[m
                 raise HTTPException(status_code=400, detail=f"Image file not found or invalid: {image_name}")[m
 [m
[31m-            photo_endpoint = f"https://graph.facebook.com/v16.0/{page_id}/photos"[m
[31m-[m
[31m-            with open(image_path, "rb") as fh:[m
[31m-                files = {"source": (image_name, fh, "image/jpeg")}[m
[31m-                data = {"published": "false", "access_token": page_token}[m
[31m-                resp = requests.post(photo_endpoint, files=files, data=data, timeout=60)[m
[31m-[m
[31m-            try:[m
[31m-                resp_json = resp.json()[m
[31m-            except Exception:[m
[31m-                raise HTTPException(status_code=502, detail=f"Facebook photo upload failed for {image_name}: {resp.text}")[m
[31m-[m
[31m-            # Debug: print photo upload outcome (filename, HTTP status, JSON response)[m
[31m-            try:[m
[31m-                print(f"facebook.photo_upload filename={image_name} status={resp.status_code} response={resp_json}", flush=True)[m
[31m-            except Exception:[m
[31m-                pass[m
[31m-[m
[31m-            if resp.status_code != 200 or "id" not in resp_json:[m
[31m-                raise HTTPException(status_code=502, detail=f"Facebook photo upload failed for {image_name}: {resp_json}")[m
[31m-[m
[31m-            photo_fb_ids.append(resp_json["id"])[m
[31m-[m
[31m-        # Create feed post with attached_media if any[m
[31m-        feed_endpoint = f"https://graph.facebook.com/v16.0/{page_id}/feed"[m
[31m-        data = {"message": message, "access_token": page_token}[m
[31m-[m
[31m-        if photo_fb_ids:[m
[31m-            attached = [{"media_fbid": fid} for fid in photo_fb_ids][m
[31m-            # Debug: print attached_media structure (do not include tokens)[m
[31m-            try:[m
[31m-                print(f"facebook.attached_media {attached}", flush=True)[m
[31m-            except Exception:[m
[31m-                pass[m
[31m-            data["attached_media"] = json.dumps(attached)[m
[31m-[m
[31m-        resp = requests.post(feed_endpoint, data=data, timeout=30)[m
[31m-        try:[m
[31m-            resp_json = resp.json()[m
[31m-        except Exception:[m
[31m-            raise HTTPException(status_code=502, detail=f"Facebook feed creation failed: {resp.text}")[m
[31m-[m
[31m-        # Debug: print feed response (status and JSON)[m
[31m-        try:[m
[31m-            print(f"facebook.feed_create status={resp.status_code} response={resp_json}", flush=True)[m
[31m-        except Exception:[m
[31m-            pass[m
[32m+[m[32m        # Generate a cryptographically secure media token for this job's images[m
[32m+[m[32m        media_token = generate_media_token(job_id, image_ids)[m
[32m+[m
[32m+[m[32m        # Build Buffer asset URLs using the public media token[m
[32m+[m[32m        assets = [][m
[32m+[m[32m        if image_ids:[m
[32m+[m[32m            for image_name in image_ids:[m
[32m+[m[32m                url = build_buffer_media_url(request, media_token, image_name)[m
[32m+[m[32m                assets.append({"url": url})[m
[32m+[m
[32m+[m[32m        # Build Buffer GraphQL createPost mutation[m
[32m+[m[32m        # Escape the message for GraphQL string literal[m
[32m+[m[32m        escaped_message = message.replace('\\', '\\\\').replace('"', '\\"').replace('\n', '\\n')[m
[32m+[m
[32m+[m[32m        # Build the assets array for GraphQL[m
[32m+[m[32m        assets_graphql = ""[m
[32m+[m[32m        if assets:[m
[32m+[m[32m            asset_entries = [f'{{url: "{asset["url"]}"}}'for asset in assets][m
[32m+[m[32m            assets_graphql = f", assets: [{', '.join(asset_entries)}]"[m
[32m+[m
[32m+[m[32m        mutation = f'''mutation {{[m
[32m+[m[32m            createPost(input: {{[m
[32m+[m[32m                text: "{escaped_message}",[m
[32m+[m[32m                channelId: "{channel_id}",[m
[32m+[m[32m                schedulingType: automatic,[m
[32m+[m[32m                mode: shareNow,[m
[32m+[m[32m                metadata: {{ facebook: {{ type: post }} }}{assets_graphql}[m
[32m+[m[32m            }}) {{[m
[32m+[m[32m                success[m
[32m+[m[32m                id[m
[32m+[m[32m            }}[m
[32m+[m[32m        }}'''[m
[32m+[m
[32m+[m[32m        # Call Buffer GraphQL API[m
[32m+[m[32m        gql_response = call_buffer_graphql(api_key, mutation)[m
[32m+[m
[32m+[m[32m        # Check for GraphQL errors[m
[32m+[m[32m        if "errors" in gql_response and gql_response["errors"]:[m
[32m+[m[32m            errors = gql_response["errors"][m
[32m+[m[32m            error_msg = "; ".join([str(e.get("message", str(e))) for e in errors])[m
[32m+[m[32m            raise HTTPException(status_code=502, detail=f"Buffer GraphQL error: {error_msg}")[m
[32m+[m
[32m+[m[32m        # Extract response data[m
[32m+[m[32m        if "data" not in gql_response or not gql_response["data"]:[m
[32m+[m[32m            raise HTTPException(status_code=502, detail="Buffer API returned no data.")[m
[32m+[m
[32m+[m[32m        data = gql_response["data"][m
[32m+[m[32m        if "createPost" not in data:[m
[32m+[m[32m            raise HTTPException(status_code=502, detail="Buffer API response missing createPost.")[m
[32m+[m
[32m+[m[32m        create_post_result = data["createPost"][m
[32m+[m[32m        if not create_post_result.get("success"):[m
[32m+[m[32m            raise HTTPException([m
[32m+[m[32m                status_code=502,[m
[32m+[m[32m                detail=f"Buffer API createPost failed: {create_post_result}"[m
[32m+[m[32m            )[m
 [m
[31m-        if resp.status_code != 200 or "id" not in resp_json:[m
[31m-            raise HTTPException(status_code=502, detail=f"Facebook feed creation failed: {resp_json}")[m
[32m+[m[32m        post_id = create_post_result.get("id")[m
[32m+[m[32m        if not post_id:[m
[32m+[m[32m            raise HTTPException(status_code=502, detail="Buffer API response missing post ID.")[m
 [m
[31m-        # Construct a readable post URL from the returned id (expected format: PAGEID_POSTID)[m
[31m-        post_full_id = resp_json.get("id")[m
[31m-        post_url = None[m
[31m-        if post_full_id:[m
[31m-            parts = str(post_full_id).split("_", 1)[m
[31m-            if len(parts) == 2:[m
[31m-                page_part, post_part = parts[0], parts[1][m
[31m-            else:[m
[31m-                page_part = page_id[m
[31m-                post_part = parts[0][m
[31m-[m
[31m-            post_url = f"https://www.facebook.com/{page_part}/posts/{post_part}"[m
[31m-[m
[31m-        return {"success": True, "post_id": post_full_id, "post_url": post_url}[m
[32m+[m[32m        # Return response compatible with frontend (success, post_id, post_url as null)[m
[32m+[m[32m        return {"success": True, "post_id": post_id, "post_url": None}[m
 [m
     except HTTPException:[m
         raise[m
[1mdiff --git a/external-script/encar_download2.py b/external-script/encar_download2.py[m
[1mindex f2a4b2a..40d4296 100644[m
[1m--- a/external-script/encar_download2.py[m
[1m+++ b/external-script/encar_download2.py[m
[36m@@ -288,7 +288,7 @@[m [mJSON формат:[m
         return "\n".join(lines)[m
 [m
 SAVE_DIR = "encar_images"[m
[31m-MAX_IMAGES = 15[m
[32m+[m[32mMAX_IMAGES = 12[m
 OPENAI_MODEL = os.getenv("OPENAI_MODEL", "gpt-5-mini")[m
 [m
 client = OpenAI(api_key=os.getenv("OPENAI_API_KEY"))[m
[36m@@ -301,7 +301,7 @@[m [mheaders = {[m
 STATIC_FOOTER = """━━━━━━━━━━━━━━━━━━━[m
 ✅ Възможност за лизинг без първоначална вноска[m
 ✅ VIN проверка + пълен списък с екстри при интерес[m
[31m-✅ Доставка до 3–4 месеца – всичко организирано от нас[m
[32m+[m[32m✅ Доставка до 3 месеца – всичко организирано от нас[m
 ✅ Пълно съдействие до регистрация в България[m
 ━━━━━━━━━━━━━━━━━━━[m
 📲 Следвайте ни във Facebook за още предложения[m
